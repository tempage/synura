# به Synura خوش آمدید!

## Synura چیست؟
سینورا (Synura) یک برنامه همه کاره است که به شما امکان می‌دهد با استفاده از برنامه‌های کوچک قدرتمندی به نام "افزونه‌ها" (extensions)، محتوا را از منابع مختلف مرور کنید. به آن مانند یک مرورگر فکر کنید، اما به جای وب‌سایت‌ها، از افزونه‌ها برای دریافت محتوا در قالبی تمیز و بومی استفاده می‌کنید.

## مفاهیم کلیدی برای کاربران

*   **کشف افزونه**: دامنه‌ای (مانند `example.com` یا `https://example.com`) را وارد کنید تا فایل `synura.js` را به طور خودکار از آن دامنه دریافت کنید. اگر پروتکلی ارائه نشود، به طور پیش‌فرض از `https://` استفاده می‌شود. این روش اصلی برای نصب افزونه‌ها از وب‌سایت‌های رسمی آن‌ها است.
*   **نصب مستقیم**: یک URL کامل (مانند `https://raw.githubusercontent.com/user/repo/main/synura.js`) را برای نصب یک اسکریپت افزونه خاص وارد کنید. **نکته امنیتی**: این روش برای جلوگیری از اجرای کد مخرب به دامنه‌های مورد اعتماد (مانند GitHub، GitLab و غیره) محدود شده است. از این روش برای دامنه‌های عمومی استفاده نکنید.
*   **اعتبارسنجی لیست سفید**: نصب‌های مستقیم URL برای امنیت در برابر لیست سفیدی از دامنه‌های مجاز اعتبارسنجی می‌شوند. کشف دامنه برای اجازه دادن به کاوش، این بررسی را دور می‌زند.
*   **افزونه‌ها**: این‌ها پلاگین‌های کوچکی هستند که محتوا را دریافت و نمایش می‌دهند. برای مثال، می‌توانید افزونه‌ای برای یک سایت خبری، یک پلتفرم ویدیویی یا فید رسانه‌های اجتماعی داشته باشید. می‌توانید افزونه‌های جدیدی نصب کنید تا کارهایی که می‌توانید با Synura انجام دهید را گسترش دهید.
*   **زمان‌های اجرا (Runtimes)**: وقتی افزونه‌ای را باز می‌کنید، در یک "زمان اجرا" اجرا می‌شود. می‌توانید چندین زمان اجرا را همزمان باز داشته باشید، درست مانند داشتن چندین زبانه در یک مرورگر وب. هر زمان اجرا نمونه‌ای جداگانه از یک افزونه است. می‌توانید بین آن‌ها جابجا شوید و حتی چندین زمان اجرا برای یک افزونه داشته باشید.
*   **نشانک‌ها**: چیز جالبی پیدا کردید؟ می‌توانید نمای فعلی را نشانک‌گذاری کنید تا بعداً آن را ذخیره کنید. یک نشانک وضعیت دقیق نما را ذخیره می‌کند، بنابراین می‌توانید هر زمان که بخواهید به آن بازگردید.

## گشت و گذار در برنامه

### صفحه اصلی
صفحه اصلی برنامه جایی است که زمان‌های اجرای خود را مدیریت می‌کنید. نوار بالایی (نوار برنامه) ابزار اصلی ناوبری شماست.

### نوار برنامه

نوار برنامه دارای چندین نماد است که به شما در ناوبری و مدیریت محتوایتان کمک می‌کند. برخی از نمادها دارای **میانبرهای مخفی** هستند که با فشار طولانی قابل دسترسی هستند:

*   **`+` (افزودن)**:
    *   **ضربه**: باز کردن یک زمان اجرای جدید. می‌توانید یک افزونه نصب شده را انتخاب کنید یا دامنه/URL وب‌سایتی را برای نصب افزونه جدید وارد کنید.
    *   **فشار طولانی**: باز کردن صفحه **مدیریت افزونه** برای مشاهده جزئیات مربوط به افزونه‌های نصب شده خود.
*   **منوی کشویی (مرکز)**: زمان اجرای فعال فعلی را نمایش می‌دهد. برای جابجایی بین زمان‌های اجرای باز ضربه بزنید، یا برای چرخیدن بین آن‌ها روی منوی کشویی به چپ/راست بکشید.
*   **`X` (بستن)**: زمان اجرای فعلی را می‌بندد.
*   **`↻` (به‌روزرسانی)**: *فقط در حالت توسعه‌دهنده قابل مشاهده است.* افزونه فعلی را از منبع آن به‌روزرسانی می‌کند.
*   **`✨` (هوش مصنوعی)**:
    *   **ضربه**: باز کردن **منوی هوش مصنوعی** برای اقدامات سریع (خلاصه، ترجمه و غیره).
    *   **فشار طولانی**: باز کردن **تنظیمات هوش مصنوعی** برای پیکربندی ارائه‌دهندگان و اولویت‌ها.
*   **`☆` (افزودن نشانک)**:
    *   **ضربه**: ذخیره نمای فعلی در نشانک‌های خود.
    *   **فشار طولانی**: رفتن مستقیم به لیست **نشانک‌ها**.
*   **`🔖` (نشانک‌ها)**: مشاهده لیست نشانک‌های ذخیره شده شما.
*   **`⚙️` (تنظیمات)**: باز کردن صفحه تنظیمات اصلی.

اگر صفحه خیلی باریک باشد، برخی از گزینه‌ها ممکن است به یک منوی سه نقطه منتقل شوند.

### دکمه هوش مصنوعی (`✨`)
روی **دکمه هوش مصنوعی** در نوار برنامه ضربه بزنید تا **گفتگوی منوی هوش مصنوعی** باز شود. این به شما ویژگی‌های مبتنی بر هوش مصنوعی را در صورت تقاضا برای نمای فعلی می‌دهد:

*   **خلاصه**: دریافت یک خلاصه سریع تولید شده توسط هوش مصنوعی از محتوای روی صفحه.
*   **ترجمه**: ترجمه محتوا به زبان مقصد شما (که در تنظیمات هوش مصنوعی پیکربندی شده است).
*   **فرمان سفارشی**: دستورالعمل‌های خود را برای تجزیه و تحلیل محتوا توسط هوش مصنوعی وارد کنید.
*   **اشتراک‌گذاری در هوش مصنوعی خارجی**: صادرات محتوای نمای فعلی به برنامه‌های هوش مصنوعی خارجی مانند ChatGPT یا Gemini در دستگاه خود.
*   **تغییر وضعیت کش**: کنترل اینکه آیا از نتایج هوش مصنوعی ذخیره شده استفاده شود یا تجزیه و تحلیل جدیدی اعمال شود.

برای پیکربندی دقیق هوش مصنوعی، به **تنظیمات > تنظیمات هوش مصنوعی** بروید که در آن می‌توانید:
*   ارائه‌دهنده هوش مصنوعی مورد نظر خود را پیکربندی کنید (Gemini، OpenAI، DeepSeek، Claude).
*   زبان‌های مبدأ و مقصد را برای ترجمه تنظیم کنید.
*   محدوده جستجوی تجزیه و تحلیل (عمیق فقط در نمای لیست موجود است) و نمایه (خلاصه، توضیح، ساده‌سازی، بررسی واقعیت، نقد، بینش) را انتخاب کنید.
*   اولویت‌های طول خلاصه را تنظیم کنید.
*   آمار استفاده از توکن را مشاهده کنید.
*   کلیدهای API را برای هر ارائه‌دهنده مدیریت کنید.

### نشانک‌ها
صفحه نشانک‌ها تمام نماهای ذخیره شده شما را نشان می‌دهد.

*   **عکس فوری نما**: ضربه زدن روی یک نشانک، یک **عکس فوری ذخیره شده** از صفحه را همانطور که در زمان ذخیره بود باز می‌کند. این برای مراجعه سریع به اطلاعات بدون نیاز به اتصال به اینترنت عالی است.
*   **بازیابی نما**: برای تعامل مجدد با صفحه (به عنوان مثال، کلیک کردن روی پیوندها، به‌روزرسانی داده‌ها)، به دنبال **نماد بازیابی** بگردید. ضربه زدن روی این، دوباره به افزونه متصل می‌شود و نما را در یک زمان اجرای جدید زنده می‌کند.

## تنظیمات (`⚙️`)

صفحه تنظیمات به شما امکان می‌دهد تقریباً هر جنبه‌ای از تجربه Synura خود را دقیق تنظیم کنید.

### افزونه‌ها
*   **نصب افزونه‌های جدید**: روی دکمه **`+`** در نوار برنامه ضربه بزنید و دامنه وب‌سایت را وارد کنید (به عنوان مثال، `https://example.com`). اگر سایت از Synura پشتیبانی کند، افزونه به طور خودکار کشف و نصب می‌شود.
*   **مدیریت افزونه‌ها**: روی **مدیریت** ضربه بزنید تا لیستی از افزونه‌های نصب شده خود را ببینید، جایی که می‌توانید آن‌ها را به‌روزرسانی یا حذف کنید.

### ظاهر
*   **تنظیم تراکم محتوا**: از نوار لغزنده استفاده کنید تا محتوا را بازتر یا فشرده‌تر نشان دهید. پیش‌نمایش زنده‌ای از تأثیر آن بر لیست‌ها و کارت‌ها خواهید دید.
*   **تم رنگ**: با انتخاب بین طرح‌های رنگی **روشن**، **تاریک** و **Monokai**، ظاهر برنامه را شخصی‌سازی کنید.
*   **وزن قلم**: ضخامت متن را به دلخواه خود تنظیم کنید (به عنوان مثال، سبک، معمولی، ضخیم).
*   **زبان**: زبان برنامه را تنظیم کنید. می‌توانید یک زبان خاص را انتخاب کنید یا اجازه دهید از پیش‌فرض سیستم شما پیروی کند.

### رفتار
*   **مهلت زمانی شبکه**: مدت زمانی را که برنامه باید منتظر پاسخ از یک درخواست شبکه باشد، از 1 تا 60 ثانیه تنظیم کنید.
*   **تنظیمات پروکسی**: یک سرور پروکسی برای درخواست‌های شبکه پیکربندی کنید.
*   **تنظیمات کش**: کش برنامه را مدیریت کنید، از جمله پاک کردن داده‌های ذخیره شده برای آزاد کردن فضا.
*   **انیمیشن GIF**: کنترل نحوه پخش GIFهای متحرک: **خاموش** (تصویر ثابت)، **یک بار** (پخش یک بار)، یا **حلقه** (پخش مداوم).

### ویدیو و صدا
*   **پخش خودکار ویدیو**: سوئیچی برای کنترل اینکه آیا ویدیوها وقتی روی صفحه ظاهر می‌شوند به طور خودکار پخش شوند یا خیر.
*   **پخش پس‌زمینه ویدیو**: این را فعال کنید تا حتی پس از دور شدن یا جابجایی به برنامه دیگر، صدای ویدیو را بشنوید.
*   **ترکیب با دیگران**: اجازه دهید صدای Synura همزمان با صدای سایر برنامه‌ها پخش شود.
*   **ساعات DVR پخش زنده**: برای پخش‌های زنده، انتخاب کنید که چند ساعت از پخش برای جستجو به عقب در دسترس باشد (از 0 تا 6 ساعت).

### حریم خصوصی و امنیت
*   **مدیریت تنظیمات**: گزینه‌های مختلف حریم خصوصی و امنیتی را برای کنترل داده‌های ذخیره شده و اشتراک‌گذاری شده پیکربندی کنید.

### درباره
*   **مجوزهای متن باز**: مجوزهای نرم‌افزار متن باز که به قدرت بخشیدن به Synura کمک می‌کند را مشاهده کنید.

---
*این سند برای کاربران نهایی است. برای مستندات توسعه‌دهنده، لطفاً به [شروع کار](getting_started.md)، [مرجع API](api_reference.md) و [مثال‌ها](examples.md) مراجعه کنید.*