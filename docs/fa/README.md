# به Synura خوش آمدید!

## Synura چیست؟
سینورا (Synura) یک برنامه چندمنظوره است که به شما امکان می‌دهد با استفاده از مینی‌اپ‌های قدرتمندی به نام "افزونه‌ها" (extensions)، محتوا را از منابع مختلف مرور کنید. آن را مانند یک مرورگر در نظر بگیرید، اما به جای وب‌سایت‌ها، از افزونه‌ها برای دریافت محتوا در قالبی تمیز و بومی استفاده می‌کنید.

## مفاهیم اصلی برای کاربران

*   **کشف افزونه**: یک دامنه (مانند `example.com` یا `https://example.com`) را وارد کنید تا به طور خودکار فایل `synura.js` از آن دامنه دریافت شود. اگر پروتکل ارائه نشود، به طور پیش‌فرض از `https://` استفاده می‌شود. این روش اصلی برای نصب افزونه‌ها از وب‌سایت‌های رسمی آن‌هاست.
*   **نصب مستقیم**: یک آدرس اینترنتی کامل (مانند `https://raw.githubusercontent.com/user/repo/main/synura.js`) را برای نصب یک اسکریپت افزونه خاص وارد کنید. **نکته امنیتی**: این روش برای جلوگیری از اجرای کدهای مخرب به دامنه‌های قابل اعتماد (مانند GitHub، GitLab و غیره) محدود شده است. از این روش برای دامنه‌های عمومی استفاده نکنید.
*   **تایید لیست سفید**: نصب‌های مستقیم URL برای امنیت در برابر یک لیست سفید از دامنه‌های مجاز تایید می‌شوند. کشف دامنه برای اجازه دادن به کاوش، این بررسی را دور می‌زند.
*   **افزونه‌ها**: این‌ها پلاگین‌های کوچکی هستند که محتوا را دریافت و نمایش می‌دهند. برای مثال، می‌توانید یک افزونه برای یک سایت خبری، یک پلتفرم ویدیویی یا یک فید رسانه اجتماعی داشته باشید. شما می‌توانید افزونه‌های جدیدی نصب کنید تا کارهایی که می‌توانید با Synura انجام دهید را گسترش دهید.
*   **ران‌تایم‌ها (Runtimes)**: وقتی یک افزونه را باز می‌کنید، در یک "ران‌تایم" اجرا می‌شود. شما می‌توانید چندین ران‌تایم را به طور همزمان باز داشته باشید، درست مانند داشتن چندین تب در یک مرورگر وب. هر ران‌تایم یک نمونه جداگانه از یک افزونه است. شما می‌توانید بین آن‌ها جابجا شوید و حتی چندین ران‌تایم برای یک افزونه داشته باشید.
*   **نشانک‌ها**: چیز جالبی پیدا کردید؟ می‌توانید نمای فعلی را نشانک‌گذاری کنید تا بعداً آن را ذخیره کنید. یک نشانک وضعیت دقیق نما را ذخیره می‌کند، بنابراین می‌توانید هر زمان که بخواهید به آن بازگردید.

## گشت و گذار در برنامه

### صفحه اصلی
صفحه اصلی برنامه جایی است که ران‌تایم‌های خود را مدیریت می‌کنید. نوار بالایی (نوار برنامه) ابزار اصلی ناوبری شماست.

### نوار برنامه

نوار برنامه دارای چندین آیکون است:

*   **`+` (افزودن)**: برای باز کردن یک ران‌تایم جدید روی این ضربه بزنید. می‌توانید یک افزونه نصب شده را انتخاب کنید یا یک دامنه وب‌سایت را برای نصب یک افزونه جدید وارد کنید.
*   **منوی کشویی (مرکز)**: این ران‌تایم فعال فعلی را نشان می‌دهد. برای دیدن لیستی از تمام ران‌تایم‌های باز و جابجایی بین آن‌ها، روی آن ضربه بزنید. همچنین می‌توانید روی منوی کشویی به چپ یا راست بکشید تا سریع جابجا شوید.
*   **`X` (بستن)**: این ران‌تایم فعلی را می‌بندد.
*   **`☆` (افزودن نشانک)**: برای ذخیره نمای فعلی در نشانک‌های خود روی این ضربه بزنید.
*   **`🔖` (نشانک‌ها)**: این شما را به لیست نشانک‌های ذخیره شده می‌برد.
*   **`⚙️` (تنظیمات)**: این صفحه تنظیمات را باز می‌کند، جایی که می‌توانید Synura را سفارشی کنید.

اگر صفحه خیلی باریک باشد، این گزینه‌ها در یک منوی سه نقطه‌ای در سمت راست جمع می‌شوند.

### نشانک‌ها
صفحه نشانک‌ها تمام نماهای ذخیره شده شما را نشان می‌دهد.

*   **عکس فوری نما**: ضربه زدن روی یک نشانک یک **عکس فوری کش شده** از صفحه را همانطور که در زمان ذخیره بود باز می‌کند. این برای ارجاع سریع به اطلاعات بدون نیاز به اتصال اینترنت عالی است.
*   **بازیابی نما**: برای تعامل دوباره با صفحه (مثلاً کلیک روی لینک‌ها، به‌روزرسانی داده‌ها)، به دنبال **آیکون بازیابی** بگردید. ضربه زدن روی این، دوباره به افزونه متصل می‌شود و نما را در یک ران‌تایم جدید زنده می‌کند.

## تنظیمات (`⚙️`)

صفحه تنظیمات به شما امکان می‌دهد تقریباً تمام جنبه‌های تجربه Synura خود را تنظیم کنید.

### افزونه‌ها
*   **نصب افزونه‌های جدید**: روی دکمه **`+`** در نوار برنامه ضربه بزنید و دامنه وب‌سایت (مانند `https://example.com`) را وارد کنید. اگر سایت از Synura پشتیبانی کند، افزونه به طور خودکار کشف و نصب می‌شود.
*   **مدیریت افزونه‌ها**: روی **مدیریت** ضربه بزنید تا لیستی از افزونه‌های نصب شده خود را ببینید، جایی که می‌توانید آن‌ها را به‌روزرسانی یا حذف کنید.

### ظاهر
*   **تنظیم تراکم محتوا**: از نوار لغزنده استفاده کنید تا محتوا بازتر یا فشرده‌تر به نظر برسد. پیش‌نمایش زنده‌ای از تأثیر آن بر لیست‌ها و کارت‌ها خواهید دید.
*   **تم رنگ**: ظاهر برنامه را با انتخاب بین طرح‌های رنگی **روشن**، **تاریک** و **Monokai** شخصی‌سازی کنید.
*   **وزن قلم**: ضخامت متن را به دلخواه خود تنظیم کنید (مانند سبک، معمولی، ضخیم).
*   **زبان**: زبان برنامه را تنظیم کنید. می‌توانید یک زبان خاص را انتخاب کنید یا بگذارید از پیش‌فرض سیستم شما پیروی کند.

### رفتار
*   **مهلت زمانی شبکه**: تعیین کنید که برنامه چقدر باید منتظر پاسخ از یک درخواست شبکه بماند، از ۱ تا ۶۰ ثانیه.
*   **تنظیمات پروکسی**: یک سرور پروکسی برای درخواست‌های شبکه پیکربندی کنید.
*   **تنظیمات حافظه پنهان**: حافظه پنهان برنامه را مدیریت کنید، از جمله پاک کردن داده‌های کش شده برای آزاد کردن فضا.
*   **انیمیشن GIF**: کنترل نحوه پخش GIFهای متحرک: **خاموش** (تصویر ثابت)، **یکبار** (پخش یک بار)، یا **تکرار** (پخش مداوم).

### ویدیو و صدا
*   **پخش خودکار ویدیو**: کلیدی برای کنترل اینکه آیا ویدیوها وقتی روی صفحه ظاهر می‌شوند به طور خودکار شروع به پخش کنند یا خیر.
*   **پخش پس‌زمینه ویدیو**: این را فعال کنید تا حتی پس از رفتن به جای دیگر یا جابجایی به برنامه دیگر، صدای ویدیو را بشنوید.
*   **ترکیب با دیگران**: اجازه دهید صدای Synura همزمان با صدای سایر برنامه‌ها پخش شود.
*   **ساعات DVR پخش زنده**: برای پخش‌های زنده، انتخاب کنید که چند ساعت از پخش برای جستجو به عقب در دسترس باشد (از ۰ تا ۶ ساعت).

### حریم خصوصی و امنیت
*   **مدیریت تنظیمات**: گزینه‌های مختلف حریم خصوصی و امنیت را برای کنترل داده‌هایی که ذخیره و به اشتراک گذاشته می‌شوند، پیکربندی کنید.

### درباره
*   **مجوزهای متن باز**: مجوزهای نرم‌افزار متن بازی که به قدرت بخشیدن به Synura کمک می‌کنند را مشاهده کنید.

---
*این سند برای کاربران نهایی است. برای مستندات توسعه‌دهندگان، لطفاً [شروع کار](getting_started.md)، [مرجع API](api_reference.md) و [مثال‌ها](examples.md) را ببینید.*