# ברוכים הבאים ל-Synura!

## מה זה Synura?
Synura היא אפליקציה רב-תכליתית המאפשרת לך לגלוש בתוכן ממקורות שונים באמצעות מיני-אפליקציות חזקות הנקראות "הרחבות". תחשוב על זה כדפדפן, אבל במקום אתרי אינטרנט, אתה משתמש בהרחבות כדי לקבל תוכן בפורמט נקי של אפליקציה טבעית (native).

## מושגי יסוד למשתמשים

*   **גילוי הרחבות**: הזן דומיין (למשל, `example.com` או `https://example.com`) כדי לאחזר אוטומטית את קובץ ה-`synura.js` מאותו דומיין. אם לא מסופק פרוטוקול, נעשה שימוש ב-`https://` כברירת מחדל. זו הדרך העיקרית להתקין הרחבות מאתרי האינטרנט הרשמיים שלהן.
*   **התקנה ישירה**: הזן כתובת URL מלאה (למשל, `https://raw.githubusercontent.com/user/repo/main/synura.js`) כדי להתקין סקריפט הרחבה ספציפי. **הערת אבטחה**: שיטה זו מוגבלת לדומיינים מהימנים (כמו GitHub, GitLab וכו') כדי למנוע הרצת קוד זדוני. אל תשתמש בזה עבור דומיינים כלליים.
*   **אימות רשימה לבנה**: התקנות ישירות מ-URL מאומתות מול רשימה לבנה של דומיינים מותרים למען אבטחה. גילוי דומיינים עוקף בדיקה זו כדי לאפשר חקירה.
*   **הרחבות**: אלו הם תוספים קטנים המביאים ומציגים תוכן. לדוגמה, יכולה להיות לך הרחבה לאתר חדשות, פלטפורמת וידאו, או פיד של מדיה חברתית. אתה יכול להתקין הרחבות חדשות כדי להרחיב את מה שאתה יכול לעשות עם Synura.
*   **זמני ריצה (Runtimes)**: כשאתה פותח הרחבה, היא רצה ב"זמן ריצה". יכולים להיות לך מספר זמני ריצה פתוחים בבת אחת, בדיוק כמו שיש לך מספר לשוניות בדפדפן אינטרנט. כל זמן ריצה הוא מופע נפרד של הרחבה. אתה יכול לעבור ביניהם, ואפילו שיהיו לך מספר זמני ריצה לאותה הרחבה.
*   **סימניות**: מצאת משהו מעניין? אתה יכול לשמור את התצוגה הנוכחית בסימניות כדי לשמור אותה לאחר כך. סימניה שומרת את המצב המדויק של התצוגה, כך שתוכל לחזור אליה בכל עת.

## התמצאות באפליקציה

### המסך הראשי
המסך הראשי של האפליקציה הוא המקום שבו אתה מנהל את זמני הריצה שלך. הסרגל העליון (סרגל האפליקציה) הוא כלי הניווט הראשי שלך.

### סרגל האפליקציה

לסרגל האפליקציה יש מספר סמלים שעוזרים לך לנווט ולנהל את התוכן שלך. לחלק מהסמלים יש **קיצורי דרך נסתרים** אליהם ניתן לגשת בלחיצה ארוכה:

*   **`+` (הוסף)**:
    *   **הקשה**: פתח זמן ריצה חדש. אתה יכול לבחור הרחבה מותקנת או להזין דומיין/URL של אתר כדי להתקין אחת חדשה.
    *   **לחיצה ארוכה**: פתח את מסך **ניהול ההרחבות** כדי לצפות בפרטים על ההרחבות המותקנות שלך.
*   **תפריט נפתח (מרכז)**: מציג את זמן הריצה הפעיל כעת. הקש כדי לעבור בין זמני ריצה פתוחים, או החלק שמאלה/ימינה על התפריט הנפתח כדי לגלול ביניהם.
*   **`X` (סגור)**: סוגר את זמן הריצה הנוכחי.
*   **`↻` (עדכן)**: *גלוי רק במצב מפתח.* מעדכן את ההרחבה הנוכחית מהמקור שלה.
*   **`✨` (AI)**:
    *   **הקשה**: פתח את **תפריט ה-AI** לפעולות מהירות (סיכום, תרגום וכו').
    *   **לחיצה ארוכה**: פתח את **הגדרות ה-AI** כדי להגדיר ספקים והעדפות.
*   **`☆` (הוסף סימניה)**:
    *   **הקשה**: שמור את התצוגה הנוכחית בסימניות שלך.
    *   **לחיצה ארוכה**: עבור ישירות לרשימת **הסימניות** שלך.
*   **`🔖` (סימניות)**: צפה ברשימת הסימניות השמורות שלך.
*   **`⚙️` (הגדרות)**: פתח את מסך ההגדרות הראשי.

אם המסך צר מדי, חלק מהאפשרויות עשויות לעבור לתפריט של שלוש נקודות.

### כפתור ה-AI (`✨`)
הקש על **כפתור ה-AI** בסרגל האפליקציה כדי לפתוח את **תיבת השיח של תפריט ה-AI**. זה נותן לך תכונות מבוססות AI לפי דרישה עבור התצוגה הנוכחית:

*   **סיכום**: קבל סיכום מהיר שנוצר על ידי AI של התוכן שעל המסך.
*   **תרגם**: תרגם את התוכן לשפת היעד שלך (מוגדר בהגדרות AI).
*   **פרומפט מותאם אישית**: הזן הוראות משלך ל-AI כדי לנתח את התוכן.
*   **שתף ל-AI חיצוני**: יצא את תוכן התצוגה הנוכחית לאפליקציות AI חיצוניות כמו ChatGPT או Gemini במכשיר שלך.
*   **מתג מטמון**: שלוט האם להשתמש בתוצאות AI שמורות במטמון או לכפות ניתוח חדש.

להגדרת AI מפורטת, עבור אל **הגדרות > הגדרות AI** שם תוכל:
*   להגדיר את ספק ה-AI המועדף עליך (Gemini, OpenAI, DeepSeek, Claude).
*   להגדיר שפות מקור ויעד לתרגום.
*   לבחור טווח חיפוש ניתוח (עמוק נמצא רק בתצוגת רשימה) ופרופיל (סיכום, הסבר, פישוט, בדיקת עובדות, ביקורת, תובנה).
*   להתאים את העדפות אורך הסיכום.
*   לצפות בסטטיסטיקות שימוש באסימונים (Tokens).
*   לנהל מפתחות API עבור כל ספק.

### סימניות
מסך הסימניות מציג את כל התצוגות השמורות שלך.

*   **תמונת מצב של תצוגה**: הקשה על סימניה פותחת **תמונת מצב שמורה במטמון** של הדף כפי שהיה כששמרת אותו. זה נהדר לעיון מהיר במידע ללא צורך בחיבור לאינטרנט.
*   **שחזור תצוגה**: כדי ליצור אינטראקציה עם הדף שוב (למשל, ללחוץ על קישורים, לרענן נתונים), חפש את **סמל השחזור**. הקשה על זה תתחבר מחדש להרחבה ותחזיר את התצוגה לחיים בזמן ריצה חדש.

## הגדרות (`⚙️`)

מסך ההגדרות מאפשר לך לכוונן כמעט כל היבט של חווית ה-Synura שלך.

### הרחבות
*   **התקן הרחבות חדשות**: הקש על כפתור ה-**`+`** בסרגל האפליקציה והזן את דומיין האתר (למשל, `https://example.com`). אם האתר תומך ב-Synura, ההרחבה תתגלה ותותקן אוטומטית.
*   **נהל הרחבות**: הקש על **נהל** כדי לראות רשימה של ההרחבות המותקנות שלך, שם תוכל לעדכן או להסיר אותן.

### מראה
*   **התאם צפיפות תוכן**: השתמש במחוון כדי לגרום לתוכן להיראות מרווח יותר או קומפקטי יותר. תראה תצוגה מקדימה בזמן אמת של האופן שבו זה משפיע על רשימות וכרטיסים.
*   **ערכת צבעים**: התאם אישית את מראה האפליקציה על ידי בחירה בין ערכות צבעים **בהיר**, **כהה**, ו-**Monokai**.
*   **משקל גופן**: התאם את עובי הטקסט להעדפתך (למשל, קל, רגיל, מודגש).
*   **שפה**: הגדר את שפת האפליקציה. אתה יכול לבחור שפה ספציפית או לתת לה לעקוב אחר ברירת המחדל של המערכת שלך.

### התנהגות
*   **פסק זמן רשת**: הגדר כמה זמן האפליקציה צריכה לחכות לתגובה מבקשת רשת, בין 1 ל-60 שניות.
*   **הגדרות פרוקסי**: הגדר שרת פרוקסי לבקשות רשת.
*   **הגדרות מטמון**: נהל את מטמון האפליקציה, כולל ניקוי נתונים שמורים כדי לפנות מקום.
*   **אנימציית GIF**: שלוט כיצד מנוגנים קובצי GIF מונפשים: **כבוי** (תמונה סטטית), **פעם אחת** (נגן פעם אחת), או **לולאה** (נגן ברציפות).

### וידאו ושמע
*   **הפעלה אוטומטית של וידאו**: מתג לשליטה האם סרטונים מתחילים להתנגן אוטומטית כשהם מופיעים על המסך.
*   **הפעלת וידאו ברקע**: הפעל זאת כדי להמשיך לשמוע את האודיו מסרטון גם לאחר ניווט למקום אחר או מעבר לאפליקציה אחרת.
*   **ערבב עם אחרים**: אפשר לאודיו מ-Synura להתנגן בו-זמנית עם אודיו מאפליקציות אחרות.
*   **שעות DVR של הפעלה בשידור חי**: עבור שידורים חיים, בחר כמה שעות מהשידור לשמור זמינות לחיפוש אחורה (מ-0 עד 6 שעות).

### פרטיות ואבטחה
*   **נהל הגדרות**: הגדר אפשרויות פרטיות ואבטחה שונות כדי לשלוט אילו נתונים מאוחסנים ומשותפים.

### אודות
*   **רישיונות קוד פתוח**: צפה ברישיונות התוכנה בקוד פתוח שעוזרת להפעיל את Synura.

---
*מסמך זה מיועד למשתמשי קצה. לתיעוד מפתחים, אנא עיין ב-[תחילת העבודה](getting_started.md), [מקור ה-API](api_reference.md), ו-[דוגמאות](examples.md).*